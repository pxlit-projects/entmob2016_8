<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:listItems="clr-namespace:HeadphoneApp.ViewModel.ListItems;assembly=HeadphoneApp"
  x:Class="HeadphoneApp.Views.DeviceDetailPage">
  <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />


  <StackLayout>
    <Label Text="{Binding Name}"></Label>
    <!-- Services Section -->
    <ScrollView>
      <ListView ItemsSource="{Binding Path=Services}" HasUnevenRows="True" HeightRequest="2400" ItemSelected="ListView_OnItemSelected">
        <ListView.ItemTemplate>
          <DataTemplate>
            <listItems:ServiceListItem Height="300"></listItems:ServiceListItem>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </ScrollView>
  </StackLayout>
</ContentPage>